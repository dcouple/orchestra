---
name: investigate
description: Investigate broken behavior using the shared evidence-driven method, scaling from a normal root-cause pass to a deep falsifiable experiment loop.
---

# Investigate — native Codex adapter

Treat `$ARGUMENTS` as the defect report. Follow
`.references/workflows/investigate.md` as the authoritative semantic contract.

Explicitly start exactly one native `investigator` custom agent through
collaboration tools. Never launch an agent CLI. Pass the complete defect brief,
depth, evidence, and authorization boundary; await the child report before
returning. Reuse the same role with the prior evidence ledger if the contract
requires escalation, rather than starting competing investigators.
