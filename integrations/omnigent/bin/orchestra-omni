#!/usr/bin/env python3
"""Launch Omnigent using Orchestra's reusable agent configuration."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from launcher import main

if __name__ == "__main__":
    sys.exit(main())
